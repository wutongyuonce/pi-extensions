Turn this into a tiny Python invoice utility project.

Requirements:
1. Keep pyproject.toml as-is.
2. Work only inside src/.
3. Create src/totals.py exporting:
   `sum_invoice_totals(invoices: list[dict[str, int | str]]) -> int`
4. Update src/main.py to import that function and expose `demo_total`, computed from two sample invoices.
5. Fix any diagnostics pi-lens surfaces while you work.
6. Do not add dependencies.
7. End with a short summary of what you changed.
