id: go-prefer-errors-is
valid:
  - |
    if errors.Is(err, os.ErrNotExist) {
        return nil
    }
invalid:
  - |
    if err == os.ErrNotExist {
        return nil
    }
  - |
    if err != os.ErrNotExist {
        return nil
    }
