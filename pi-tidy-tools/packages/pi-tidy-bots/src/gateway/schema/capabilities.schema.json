{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://tidy.invalid/backend-protocol/v1/capabilities.schema.json",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "text": {
          "const": true
        },
        "mediaTypes": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "maxMediaBytes": {
          "type": "integer",
          "minimum": 0
        }
      },
      "required": ["text", "mediaTypes", "maxMediaBytes"],
      "additionalProperties": false,
      "patternProperties": {
        "\\.": {
          "not": {
            "type": "object",
            "properties": {
              "required": {
                "const": true
              },
              "properties": {
                "const": {}
              }
            },
            "required": ["required"]
          }
        }
      }
    },
    "sessions": {
      "type": "object",
      "properties": {
        "load": {
          "type": "boolean"
        },
        "import": {
          "type": "boolean"
        },
        "continuity": {
          "enum": ["verified", "unverified"],
          "description": "Whether this adapter's advertised proof succeeded. Not a claim that every backend has retained-history verification."
        },
        "proof": {
          "enum": ["none", "identity-only", "retained-history"],
          "description": "What a successful load actually proved. identity-only is not Pi/Hermes-equivalent history verification."
        },
        "emptySeat": {
          "enum": ["non-restorable", "restartable"],
          "description": "Never-prompted seats may remain explicitly non-restorable. Fail closed rather than invent restart-availability."
        }
      },
      "required": ["load", "import", "continuity"],
      "additionalProperties": false,
      "patternProperties": {
        "\\.": {
          "not": {
            "type": "object",
            "properties": {
              "required": {
                "const": true
              },
              "properties": {
                "const": {}
              }
            },
            "required": ["required"]
          }
        }
      },
      "allOf": [
        {
          "if": {
            "anyOf": [
              {
                "properties": {
                  "load": {
                    "const": true
                  }
                },
                "required": ["load"]
              },
              {
                "properties": {
                  "import": {
                    "const": true
                  }
                },
                "required": ["import"]
              }
            ]
          },
          "then": {
            "required": ["proof", "emptySeat"],
            "properties": {
              "continuity": {
                "const": "verified"
              },
              "proof": {
                "enum": ["identity-only", "retained-history"]
              },
              "emptySeat": {
                "enum": ["non-restorable", "restartable"]
              }
            }
          }
        }
      ]
    },
    "output": {
      "type": "object",
      "properties": {
        "text": {
          "enum": ["final-only", "snapshots"]
        },
        "tools": {
          "type": "boolean"
        },
        "usage": {
          "enum": ["reported", "estimated", "unknown"]
        }
      },
      "required": ["text", "tools", "usage"],
      "additionalProperties": false,
      "patternProperties": {
        "\\.": {
          "not": {
            "type": "object",
            "properties": {
              "required": {
                "const": true
              },
              "properties": {
                "const": {}
              }
            },
            "required": ["required"]
          }
        }
      }
    },
    "operations": {
      "type": "object",
      "properties": {
        "nativeDedupe": {
          "enum": ["durable", "none"]
        },
        "nativeDedupeRetentionMs": {
          "type": "integer",
          "minimum": 1
        },
        "nativeReplay": {
          "enum": ["cursor", "none"]
        },
        "nativeReplayRetentionMs": {
          "type": "integer",
          "minimum": 1
        },
        "nativeReplayGapSemantics": {
          "const": "explicit-gap"
        },
        "cancel": {
          "enum": ["cooperative", "process-termination", "unsupported"]
        },
        "steer": {
          "type": "boolean"
        }
      },
      "required": ["nativeDedupe", "nativeReplay", "cancel", "steer"],
      "additionalProperties": false,
      "patternProperties": {
        "\\.": {
          "not": {
            "type": "object",
            "properties": {
              "required": {
                "const": true
              },
              "properties": {
                "const": {}
              }
            },
            "required": ["required"]
          }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "nativeDedupe": {
                "const": "durable"
              }
            },
            "required": ["nativeDedupe"]
          },
          "then": {
            "required": ["nativeDedupeRetentionMs"],
            "properties": {
              "nativeDedupeRetentionMs": {}
            }
          }
        },
        {
          "if": {
            "properties": {
              "nativeReplay": {
                "const": "cursor"
              }
            },
            "required": ["nativeReplay"]
          },
          "then": {
            "required": ["nativeReplayRetentionMs", "nativeReplayGapSemantics"],
            "properties": {
              "nativeReplayRetentionMs": {},
              "nativeReplayGapSemantics": {}
            }
          }
        }
      ]
    },
    "interactions": {
      "type": "object",
      "properties": {
        "permissions": {
          "enum": ["exact-request", "none"]
        },
        "questions": {
          "type": "boolean"
        }
      },
      "required": ["permissions", "questions"],
      "additionalProperties": false,
      "patternProperties": {
        "\\.": {
          "not": {
            "type": "object",
            "properties": {
              "required": {
                "const": true
              },
              "properties": {
                "const": {}
              }
            },
            "required": ["required"]
          }
        }
      }
    },
    "configuration": {
      "type": "object",
      "properties": {
        "model": {
          "type": "boolean"
        },
        "thinking": {
          "type": "boolean"
        },
        "compact": {
          "type": "boolean"
        },
        "new_context": {
          "type": "boolean",
          "description": "Optional. Advertise a no-summary context-reset control distinct from configuration.compact. Absent means unsupported."
        }
      },
      "required": ["model", "thinking", "compact"],
      "additionalProperties": false,
      "patternProperties": {
        "\\.": {
          "not": {
            "type": "object",
            "properties": {
              "required": {
                "const": true
              },
              "properties": {
                "const": {}
              }
            },
            "required": ["required"]
          }
        }
      }
    },
    "fleetTools": {
      "type": "boolean"
    }
  },
  "required": [
    "input",
    "sessions",
    "output",
    "operations",
    "interactions",
    "configuration",
    "fleetTools"
  ],
  "additionalProperties": false,
  "patternProperties": {
    "\\.": {
      "not": {
        "type": "object",
        "properties": {
          "required": {
            "const": true
          },
          "properties": {
            "const": {}
          }
        },
        "required": ["required"]
      }
    }
  }
}
